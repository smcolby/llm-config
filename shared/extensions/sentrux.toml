name = "sentrux"

# MCP-only tool — no install / hook / symlink wiring; just MCP server registration
# per harness opt-in via mechanisms = ["mcp"]
[mcp]
command = "sentrux"
args = ["--mcp"]

[harnesses.copilot]
mechanisms = ["mcp"]
verify_mcp = "~/.copilot/mcp-config.json"
